---
pagination:
  data: collections.tagList
  size: 1
  alias: tagInfo
layout: "base.njk"
# [OPTIMIZATION] Use pre-computed slug instead of | slug filter
permalink: "/tags/{{ tagInfo.slug }}/"
---

<div class="container" data-pagefind-ignore>

<div class="home-layout">

<h1>标签：{{ tagInfo.name }}</h1>

<ul>
  {%- for item in tagInfo.posts | sort(true, false, 'date') -%}
    <li>
      {# 第一行: 标题 + 徽章 + Speaker + 日期 #}
      <a href="{{ item.url | url }}">{{ item.data.title }}</a>
      {# 徽章部分 #}
      {% if item.filePathStem.includes('/posts/') %}
        <span class="badge">✍️</span>
      {% elif item.filePathStem.includes('/notes/') %}
        <span class="badge">📝</span>
      {% endif %}
      {# Speaker 部分 #}
      {% if item.data.speaker %}
        <a href="{{ ('/speakers/' + (item.data.speaker | getSpeakerUniqueKey(collections) | slug) + '/') | url }}" class="speaker-inline">🎙️ {{ item.data.speaker }}</a>
      {% endif %}
      {# 日期部分 #}
      {% if item.date %}
        <time datetime="{{ item.date.toISOString() }}">
          {{ item.date.toLocaleDateString() }}
        </time>
      {% endif %}
      <br>

      {# 领域、分类和专题部分 - 显示在下一行 (v7.4) - 和首页保持一致 #}
      {% if item.data.area or item.data.category or (item.data.project and item.data.project.length > 0) %}
        <div class="taxonomy-line">
          {% if item.data.area %}
            <a href="{{ ('/areas/' + (item.data.area | getAreaUniqueKey(collections) | slug) + '/') | url }}" class="area-badge-inline">
              🌐 {{ item.data.area }}
            </a>
          {% endif %}
          {% if item.data.category %}
            <a href="{{ ('/categories/' + (item.data.category | slug) + '/') | url }}" class="category-badge-inline">
              🏷️ {{ item.data.category }}
            </a>
          {% endif %}
          {% if item.data.project and item.data.project.length > 0 %}
            {% for proj in item.data.project %}
              <a href="{{ ('/projects/' + (proj | slug) + '/') | url }}" class="project-badge-inline">
                📂 {{ proj }}
              </a>
            {% endfor %}
          {% endif %}
        </div>
      {% endif %}

      {# 摘要部分 - 显示内容摘要 #}
      {% if item.data.summary %}
        <small class="summary-text">📄 {{ item.data.summary }}</small>
      {% endif %}

      {# 洞察部分 - 显示为什么观看这个视频或听这个播客 #}
      {% if item.data.insight %}
        <small class="insight-text">💡 {{ item.data.insight }}</small>
      {% endif %}
    </li>
  {%- endfor -%}
</ul>

</div>

</div>