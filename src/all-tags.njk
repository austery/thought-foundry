---
title: "所有标签"
layout: "base.njk"
---

<h1>所有标签</h1>

<div class="tag-list">
  {# collections.tagList 现在是一个简单的数组，我们可以直接遍历它 #}
  {% for tagInfo in collections.tagList %}
    <a href="/tags/{{ tagInfo.key | slug }}/" class="tag-pill">
      #{{ tagInfo.name }} ({{ tagInfo.posts.length }})
    </a>
  {% endfor %}
</div>