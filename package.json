{
  "name": "thought-foundry",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "NODE_OPTIONS=\"--max-old-space-size=8192\" pnpm exec @11ty/eleventy && pnpm exec pagefind --site _site",
    "dev": "NODE_OPTIONS=\"--max-old-space-size=8192\" pnpm exec @11ty/eleventy --serve",
    "dev:incremental": "NODE_OPTIONS=\"--max-old-space-size=8192\" pnpm exec @11ty/eleventy --serve --incremental",
    "build:debug": "DEBUG=true pnpm exec @11ty/eleventy && pnpm exec pagefind --site _site",
    "build:incremental": "pnpm exec @11ty/eleventy --incremental && pnpm exec pagefind --site _site",
    "preinstall": "npx only-allow pnpm"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/austery/thought-foundry.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/austery/thought-foundry/issues"
  },
  "homepage": "https://github.com/austery/thought-foundry#readme",
  "description": "",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@sindresorhus/slugify": "^2.2.1",
    "pagefind": "^1.4.0"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "pinyin": "^4.0.0"
  }
}
